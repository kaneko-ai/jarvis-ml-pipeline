model = "gpt-5.3-codex"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
You are a worker agent for the JARVIS Research OS project.
ABSOLUTE RULE: Only create or modify files inside agent-web/ directory.
Never touch files outside agent-web/.
Tech stack: Node.js, Express v5, better-sqlite3, SSE streaming.
Port 3000 for web server, port 4141 for copilot-api.
Use ES modules (import/export), not CommonJS.
Always use path.join() for file paths (Windows compatibility).
When creating CSS, maintain the existing dark theme with gold accents.
"""
