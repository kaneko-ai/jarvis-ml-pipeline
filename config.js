/* Global config for landing page API connectivity.
 * Set `apiBaseUrl` to your deployed backend URL, for example:
 * https://jarvis-api.example.com
 */
window.JARVIS_SITE_CONFIG = Object.freeze({
    apiBaseUrl: "",
    apiHealthPath: "/api/health",
    requestTimeoutMs: 12000
});
